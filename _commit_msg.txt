补齐凭据校验并修正事务内赋值

- 新增用户名与密码的字符集和长度校验, 用户名限定字母数字下划线连字符
  且不超过 64 字符, 密码限定字母数字与常见符号且不超过 1024 字符.
  此前纯空白用户名可以落库, 创建出界面上无法复现的幽灵账号; 密码也没有
  长度上限, 而密码走 pbkdf2_hmac 十万轮, 超长密码是廉价的拒绝服务面.
  仓储层全部使用参数化查询, 不存在 SQL 注入路径, 字符集限制是纵深防御.
- _set_published() 的赋值移入事务, 避免 update() 失败时内存对象声称已发布
  而数据库并未发布.
- ServiceContainer.close() 复用 close_connection(), 消除重复逻辑.
- 修正 ensure_initial_admin() 文档中写错的异常类型.
- 删除死代码 is_configured() 及其模块级状态.
- 为八处确认不改的问题补上注释, 说明问题, 不改的理由与将来的改动条件.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
